# Colllection of datatypes in python
1. List: Heterogeneous
   1. can store name , float 10.5, number 1 2, 
   2. booloean values "true "false
   3. can store duplicate values
   4. is mutable means can be changes update delete replace add values
   5. ordered
   6. inbuilt list() 
   to initialize list(())so this is not tuple
   n=list((1,2,3)) output:[1,2,3]

2. Tuple: Immutable Data Structure

   1. immutable can be accessed but not changed
   2. ordered
   3. can store duplicates
   4. heterogeneous
   5. tuple is faster to list
   6. t=(1) not tuple but t=(1,)is tuple
   7. t=1,2,3,4 is tuple
   
3. Dictionary: Keyvalues pairs
   {}
   Dict()
   key value
   {k:v}
   empty dict -- num={}
   .items() is a built-in method used exclusively on dictionaries (dict) to retrieve both the keys and their corresponding values at the same time.

4. Sets: Stores only unique Elements 
   {}
   set() to call fn.
   unique
   {1,2,3}
   {1,2,2,3} wrong
   empty set --set()

   {()} list of tuples
   [[]] list of list
   [[],[],[]] list of list
   (()) tuple of tuple
   [{}] list of dictionary
   {[],[]} dictionary of list
   [(),{},set()]
   ([]) tuple of list
   ({}) tuple of dictionary

   all has indexing with 0 as starting
   
